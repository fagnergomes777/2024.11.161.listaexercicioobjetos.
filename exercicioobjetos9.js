// Crie um objeto chamado calculadora com os métodos somar(a, b) e multiplicar(a, b).
// Use esses métodos para somar e multiplicar dois números, e exiba os resultados no console.

const calculadora = {
    somar: (a, b) => a + b,
    multiplicar: (a, b) => a * b
  };
  
  console.log(calculadora.somar(5, 3));
  console.log(calculadora.multiplicar(5, 3));
  